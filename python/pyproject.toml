[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"

# Other versions may work, but this one defintiy does
requires-python = "==3.12.3"
dependencies = [
    "opencv-python>=4.12.0.88",
    "scikit-image>=0.25.2",
]
